import React from 'react';

export const SalesContext = React.createContext()

// export const sales_context_provider = sales_context.Provider

// export default sales_context

// export const SalesContextProvider = (props) => {
//     return(
//         <SalesContext.Provider value={[]}>
//             {props.children}
//         </SalesContext.Provider>
//     )
// }